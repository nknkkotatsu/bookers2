 <div class="container">
  <div class="row">
    <div class="col-md-3">
<%= render 'info', user:@user %>
<%= render 'books/create', books:@books %>
    </div>
    
<div class="col-md-8 offset-md-1">

<h1>Books</h1>
<!-- ユーザーの投稿一覧 -->

<div>
  <table>
    <% @books.each do |book| %>
    <tr>
      <td><%= image_tag @user.get_profile_image(100,100) %><br>
      <td><%= link_to book.title,book_path(book.id) %></td>
      <td><%= book.body %></td>
    </tr>
    <% end %>
  </table>
</div>
</div>
